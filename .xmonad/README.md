# Dependencies: (additional to the overall deps)
1. xmonad
2. xmonad-contrib
3. xmobar
4. trayer
5. ubuntu-font-family

INFO:
1. `import Colors` only works if the symlink `lib/Colors.hs to ~/.cache/wal/colors.hs` exists
